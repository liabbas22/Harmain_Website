import React from "react";

const Title = ({title}) => {
  return (
    <h2 className="text-lg font-semibold tracking-wider text-red-700 uppercase font-roboto md:text-2xl">{title}</h2>
  );
};

export default Title;
