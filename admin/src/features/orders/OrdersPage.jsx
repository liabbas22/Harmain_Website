import { ORDER_FILTERS } from "../../constants/admin";
import OrderTable from "./OrderTable";

export default function OrdersPage({ orders, filter, onFilterChange, onUpdate, onOpenOrder, busyAction, loading, error, total }) {
  return <div className="mt-6 grid gap-5"><section className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h2 className="text-base font-extrabold text-slate-900">Order queue</h2><p className="mt-1 text-sm text-slate-500">Track incoming orders and update kitchen progress.</p></div><span className="text-sm font-extrabold text-slate-500">{total} {total === 1 ? "order" : "orders"}</span></section><section className="overflow-x-auto border-b border-slate-200"><div className="flex min-w-max gap-1 px-1" role="tablist" aria-label="Order filters">{ORDER_FILTERS.map(([value, label]) => <button key={value} type="button" role="tab" aria-selected={filter === value} onClick={() => onFilterChange(value)} className={`border-b-2 px-3 py-3 text-sm font-extrabold transition ${filter === value ? "border-brand-700 text-brand-700" : "border-transparent text-slate-500 hover:border-red-200 hover:text-slate-800"}`}>{label}</button>)}</div></section>{error && <div className="border-l-4 border-brand-600 bg-red-50 px-4 py-3 text-sm font-bold text-brand-700">{error}</div>}<section className="relative overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="overflow-x-auto"><OrderTable orders={orders} onUpdate={onUpdate} onOpenOrder={onOpenOrder} busyAction={busyAction} /></div>{loading && <div className="absolute inset-x-0 top-0 h-1 overflow-hidden bg-red-100"><span className="block h-full w-1/3 animate-pulse bg-brand-700" /></div>}</section></div>;
}
